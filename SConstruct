# -*- python -*-
import os
from lsst.sconsUtils import scripts
from SCons.Script import SConscript
# Python-only package
scripts.BasicSConstruct("ci_cpp", disableCc=True, noCfgFile=True)
# SConscript(os.path.join(".", "bin.src", "SConscript"))
